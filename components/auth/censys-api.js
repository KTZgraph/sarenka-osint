//https://search.censys.io/api#/hosts/viewHost


// tODO na później
export default function searchbyIP(ipAddress, apiId, apiSecret) {
  /*
    GET https://search/censys.io/api/v2/hosts/8.8.8.8
    {"code": 401, "status": "Unauthorized", "error": "You must authenticate with a valid API ID and secret."}
    
*/


  // const auth = "Basic " + Buffer.from(apiId + ":" + apiSecret).toString("base64");

  // const headers = { Authorization: auth };
  // super({ baseUrl: BASE_URL, headers });
}
